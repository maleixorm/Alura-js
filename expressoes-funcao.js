function minhaFuncao(params) {
    // bloco de código da função
}

minhaFuncao(params);

// expressao de função

const soma = function(num1, num2) { return num1 + num2 };

console.log(soma(2, 4));

// diferença principal

// Hoisting -> o javascript lista todas as funções declaradas e as var antes de executar